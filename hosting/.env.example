# Copy to hosting/.env (gitignored). From Cloudflare Zero Trust → Networks → Tunnels → your tunnel.
TUNNEL_TOKEN=
# Optional, public: a domain-restricted Sepolia RPC URL (it ends up in the browser bundle).
VITE_SEPOLIA_RPC_URL=
